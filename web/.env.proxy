# Proxy environment variables
REACT_APP_ENV=production
# Use the CORS proxy URL (replace with your actual proxy URL after deployment)
REACT_APP_API_URL=https://envirosense-cors-proxy.onrender.com/api
# Disable CORS for development
DANGEROUSLY_DISABLE_HOST_CHECK=true
